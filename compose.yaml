services:
  aronno-landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aronno-landing
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
